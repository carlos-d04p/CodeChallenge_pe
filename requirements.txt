# ===== FairBet — dependencias de runtime =====

# Core
Django==5.2.14
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-environ==0.13.0

# Base de datos (psycopg 3)
psycopg[binary]==3.3.4

# Redis: cache + broker + channel layer
redis==7.4.0
django-redis==6.0.0

# Tiempo real (WebSockets)
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.1

# Tareas en segundo plano
celery==5.6.3

# Servidor HTTP (WSGI)
gunicorn==26.0.0

# Documentación y Esquema de API (Requisito de entrega)
drf-spectacular==0.29.0

# Pruebas de Invariantes Financieras (TDD / Property-Based)
hypothesis==6.90.0

